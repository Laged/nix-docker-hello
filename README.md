# Nix Docker Hello

Steps to Run:

1. Clone the repository:
   `git clone https://github.com/laged/nix-docker-hello && cd nix-docker-hello`

2. Start the server:
   `nix run .#server`

3. Run the client:
   `nix run .#client`
